[package]
name = "hexchain-p2p"
version = "0.9.11"
edition = "2021"
authors = ["TribeWarez"]
description = "hexchain 3D HCP lattice consensus — crystalline proof-of-work"
license = "GPL-3.0"

[dependencies]
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"

[dev-dependencies]
tempfile = "3.0"
